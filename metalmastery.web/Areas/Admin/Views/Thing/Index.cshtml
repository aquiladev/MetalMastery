@using MetalMastery.Web.App_LocalResources
@model IEnumerable<MetalMastery.Web.Areas.Admin.Models.ThingModel>

@{
    ViewBag.Title = MmResources.Things;
}

<p>
    @Html.ActionLink(MmResources.Create, "Create")
</p>
<table>
    <tr>
        <th>
            @MmResources.Name
        </th>
        <th>
            @MmResources.Description
        </th>
        <th>
            ShowOnHome
        </th>
        <th>
            @MmResources.Published
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
			@item.Name
        </td>
        <td>
			@item.Description
        </td>
        <td>
			@item.ShowOnHome
        </td>
        <td>
			@item.ShowForAll
        </td>
        <td>
            @Html.ActionLink(MmResources.Edit, "Edit", new { id=item.Id }) |
            @Html.ActionLink(MmResources.Details, "Details", new { id=item.Id }) |
            @Html.ActionLink(MmResources.Delete, "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>


