@using MetalMastery.Admin.App_LocalResources
@model MetalMastery.Admin.Models.FormatModel

@{
    ViewBag.Title = MmAdminResources.Edit;
}

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        @Html.HiddenFor(model => model.Id)
		@Html.Partial("_CreateOrEdit", Model)

        <p>
            <input type="submit" value="@MmAdminResources.Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink(MmAdminResources.BackToList, "Index")
</div>
